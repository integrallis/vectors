# Copy to .env and populate as needed. Only the Spring AI / LangChain4j notebooks
# reference these. The core, quantization, embedding-cache, and VCR notebooks
# are fully self-contained.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Jupyter Lab access token. Change before exposing the port to a network.
JUPYTER_TOKEN=java-vectors
