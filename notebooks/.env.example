# Copy to .env to select a dependency mode. All notebooks use deterministic
# local data and require no API keys or external model services.

# source: resolve the current Gradle projects; release: resolve com.integrallis from Maven Central
VECTORS_NOTEBOOK_MODE=source
VECTORS_VERSION=0.1.0
# Optional Maven repository for a locally staged release candidate.
VECTORS_NOTEBOOK_REPOSITORY=
