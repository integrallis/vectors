/**
 * Vector quantization implementations: scalar (int8/int4), product quantization (PQ), binary
 * quantization (BQ), non-uniform vector quantization (NVQ), and RaBitQ.
 */
package com.integrallis.vectors.quantization;
