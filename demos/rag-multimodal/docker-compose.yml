# docker-compose.yml for rag-multimodal demo
#
# Starts vectors-server and vectors-studio-web for the JavaFX demo to connect to.
# Studio UI is available at http://localhost:8288 to browse collections and data.
#
# Recommended — single command:
#   cd vectors/demos/rag-multimodal
#   ./run.sh
#
# Manual (two terminals):
#   cd vectors/demos/rag-multimodal
#   docker compose up -d
#   # Wait for healthy, then from vectors/:
#   ./gradlew :demos:rag-multimodal:run

services:
  vectors-server:
    build:
      context: ../..              # vectors/ directory (Gradle multi-project root)
      dockerfile: vectors-server/Dockerfile
    image: vectors-server:local
    container_name: vectors-server-demo
    ports:
      - "8287:8287"
    volumes:
      - vectors-data:/data
    environment:
      JAVA_OPTS: "-Xmx512m"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8287/v1/health"]
      interval: 10s
      timeout: 3s
      start_period: 15s
      retries: 5
    restart: unless-stopped

  vectors-studio:
    build:
      context: ../..              # vectors/ directory (Gradle multi-project root)
      dockerfile: vectors-studio-web/Dockerfile
    image: vectors-studio-web:local
    container_name: vectors-studio-demo
    ports:
      - "8288:8288"
    environment:
      JAVA_OPTS: "-Xmx256m"
    depends_on:
      vectors-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8288/health"]
      interval: 10s
      timeout: 3s
      start_period: 15s
      retries: 5
    restart: unless-stopped

volumes:
  vectors-data:
    driver: local
