/**
 * Generation segment transfer — portable ZIP-based export and import of committed generation
 * directories.
 *
 * <ul>
 *   <li>{@link com.integrallis.vectors.db.transfer.SegmentExporter} — writes a generation directory
 *       to a ZIP stream
 *   <li>{@link com.integrallis.vectors.db.transfer.SegmentImporter} — restores a ZIP stream into a
 *       target directory
 * </ul>
 */
package com.integrallis.vectors.db.transfer;
