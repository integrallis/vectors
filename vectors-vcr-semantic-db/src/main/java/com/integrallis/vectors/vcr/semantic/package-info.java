/**
 * Semantic (vector-similarity) {@link com.integrallis.vectors.vcr.CassetteStore} implementation
 * backed by a {@link com.integrallis.vectors.db.VectorCollection}.
 *
 * <p>This module is an optional add-on to {@code vectors-vcr-core}: applications that only need
 * exact key lookup (the default) do not pull in {@code vectors-db}.
 */
package com.integrallis.vectors.vcr.semantic;
