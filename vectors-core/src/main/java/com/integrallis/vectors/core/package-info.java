/**
 * SIMD distance kernels, vector types, and common abstractions for the Vectors library.
 *
 * <p>Provides distance metrics (dot product, L2, cosine similarity) accelerated via the Java Vector
 * API ({@code jdk.incubator.vector}), vector encodings (FLOAT32, INT8, BINARY), and foundational
 * data structures used across all index modules.
 */
package com.integrallis.vectors.core;
