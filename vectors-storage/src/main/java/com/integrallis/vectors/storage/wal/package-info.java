/**
 * Write-ahead log (WAL) for durable mutation journaling.
 *
 * <ul>
 *   <li>{@link com.integrallis.vectors.storage.wal.SegmentedWriteAheadLog} — rolling CRC-checked
 *       segment files
 * </ul>
 */
package com.integrallis.vectors.storage.wal;
